#include <stdio.h>

int Vowel(char c) {
    c = c | 32; 
    return (c=='a' || c=='e' || c=='i' || c=='o' || c=='u');
}

int main() {
    char str[100], result[100];
    int i, j = 0;
    
    scanf("%s", str);
    
    for(i = 0; str[i] != '\0'; i++) {
        if(!isVowel(str[i])) {
            result[j++] = str[i];
        }
    }
    result[j] = '\0';
    
    printf("%s", result);
    return 0;
}
